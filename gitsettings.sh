#!/bin/bash

# A script to set up git stuff

git config --global user.email "suomisim@outlook.com" && git config --global user.name "Simo Suominen"
git config --global credential.helper "cache --timeout=3600"
